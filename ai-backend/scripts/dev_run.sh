#!/usr/bin/env bash
# Simple dev run helper - adjust as needed
export APP_ENV=development
echo "Starting dev server (placeholder). Use uvicorn or your runner of choice."
# Example: uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
